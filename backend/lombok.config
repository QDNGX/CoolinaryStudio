# Копировать Jackson-аннотации с поля на сгенерированный геттер — иначе для boolean-поля
# с префиксом is (например isNewUser) Jackson выдаёт лишнее дублирующее свойство (newUser).
config.stopBubbling = true
lombok.copyableAnnotations += com.fasterxml.jackson.annotation.JsonProperty
